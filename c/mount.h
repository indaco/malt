// malt — sys/mount.h C interop header
// Wrapper for build-system addTranslateC (statfs + friends).

#ifndef MALT_MOUNT_H
#define MALT_MOUNT_H

#include <sys/param.h>
#include <sys/mount.h>

#endif // MALT_MOUNT_H
